"""agent_core: a transparent, framework-free agent loop.

The whole cognition layer is here — loop, providers, tools, events — with no
web-framework or database imports, so it can be tested and reused standalone.
"""

__all__ = ["__version__"]
__version__ = "0.1.0"
